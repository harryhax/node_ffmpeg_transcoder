# Packaging Status

Packaging to a standalone executable is deferred for now.

## Current Distribution Approach

Use this as a normal Node.js project:

1. Clone the repository.
2. Install dependencies with `npm install` (or run `./install.sh`).
3. Start the app with `npm run server`.

Requirements remain:

- Node.js 18+
- `ffmpeg` and `ffprobe`
